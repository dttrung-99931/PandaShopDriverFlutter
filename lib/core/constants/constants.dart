import 'package:flutter/material.dart';

class Constants {
  static const sucessfulStatusCodes = [201, 200];
  static const apiSecondsTimeout = Duration(seconds: 30);
}

const emptyWidget = SizedBox.shrink();
